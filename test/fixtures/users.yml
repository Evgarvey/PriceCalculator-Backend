one:
  id: 1
  email: user@example.com
  # Use Devise's built-in encryption for the test password
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password123') %>

two:
  id: 2
  email: user2@example.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password123') %>

